<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rotating Hammer and Sickle</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #fff;
            margin: 0;
            overflow: hidden;
        }

        .scene {
            width: 200px;
            height: 200px;
            perspective: 1000px;
        }

        .hammer-sickle {
            width: 100%;
            height: 100%;
            position: relative;
            transform-style: preserve-3d;
            animation: rotate 10s infinite linear;
        }

        @keyframes rotate {
            from { transform: rotateY(0deg); }
            to { transform: rotateY(360deg); }
        }

        .hammer {
            position: absolute;
            width: 20px;
            height: 100px;
            background-color: #ff0000;
            top: 30px;
            left: 50px;
            transform: rotate(-45deg);
        }

        .hammer-head {
            position: absolute;
            width: 60px;
            height: 20px;
            background-color: #ff0000;
            top: 0px;
            left: -20px;
        }

        .sickle {
            position: absolute;
            width: 140px;
            height: 140px;
            border: 18px solid #ff0000;
            border-radius: 50%;
            border-right-color: transparent;
            border-top-color: transparent;
            top: -10px;
            left: 30px;
            transform: rotate(-20deg);
        }

        .sickle-handle {
            position: absolute;
            width: 15px;
            height: 50px;
            background-color: #ff0000;
            top: 100px;
            left: 110px;
            transform: rotate(45deg);
        }

    </style>
</head>
<body>
    <div class="scene">
        <div class="hammer-sickle">
            <div class="hammer">
                <div class="hammer-head"></div>
            </div>
            <div class="sickle"></div>
            <div class="sickle-handle"></div>
        </div>
    </div>
</body>
</html>
